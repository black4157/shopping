<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.du.shopping.mappers.adminMapper">
	<select id="category" resultType="CategoryVO">
		select level, cateName, cateCode, cateCodeRef
		from goods_category
		start with cateCodeRef is null connect by prior cateCode = cateCodeRef
	</select>
	
	<insert id="register">
		insert into tbl_goods(gdsNum, gdsName, cateCode, gdsPrice, gdsStock, gdsDes)
		values(sequence1.nextval, #{gdsName}, #{cateCode}, #{gdsPrice}, #{gdsStock}, #{gdsDes})
	</insert>
	
	<select id="goodslist" resultType="GoodsVO">
		select gdsNum, gdsName, cateCode, gdsPrice, gdsStock, gdsDes, gdsImg, gdsDate
		from tbl_goods
		order by gdsNum desc
	</select>
	
	<select id="goodsView" resultType="GoodsVO">
		select gdsNum, gdsName, cateCode, gdsPrice, gdsStock, gdsDes, gdsImg, gdsDate
		from tbl_goods
		where gdsNum = #{gdsNum}
	</select>
</mapper>